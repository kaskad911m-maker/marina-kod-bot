# Явная команда запуска (Procfile с «worker:» Railway часто не подхватывает)
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
